/**
 * Retourne la somme de tous les nombres pairs d'un tableau.
 * @param {number[]} arr
 * @returns {number}
 */
function sumEvenNumbers(arr) {
  // Votre code ici
}

module.exports = sumEvenNumbers;
